[project]
name = "promise-ledger"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "imanax99", email = "mysteriousme42@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
promise-ledger = "promise_ledger:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"
